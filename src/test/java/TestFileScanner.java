/*
import com.khudim.scanner.FileScanner;
import com.khudim.webm.WebmService;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Configuration;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import java.io.IOException;
import java.util.Arrays;

*/
/**
 * Created by Beaver.
 *//*

@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = "classpath:test-config.xml")
@Configuration
public class TestFileScanner {

    @Autowired
    private FileScanner fileScanner;


    @Test
    public void ee() throws IOException, InterruptedException {
        Assert.assertTrue(fileScanner!=null);
        System.out.println(Arrays.toString(fileScanner.findVideoSize("D:/1.webm")));
    }
}
*/
